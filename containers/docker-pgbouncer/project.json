{
    "name": "containers-docker-pgbouncer",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "tags": ["docker", "platform:azure"],
    "sourceRoot": "packages/containers/docker-pgbouncer",
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "cache": true,
            "options": {
                "cwd": "packages/containers/docker-pgbouncer",
                "commands": ["bash ./build.sh"]
            },
            "inputs": ["default"],
            "outputs": ["{workspaceRoot}/dist/{projectRoot}/docker/**/*"]
        }
    }
}
