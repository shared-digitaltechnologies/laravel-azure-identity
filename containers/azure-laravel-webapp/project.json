{
    "name": "containers-azure-laravel-webapp",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "tags": ["docker", "platform:azure"],
    "implicitDependencies": [
        "containers-azure-otelcol-agent",
        "containers-docker-pgbouncer"
    ],
    "sourceRoot": "packages/containers/azure-laravel-webapp",
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "cache": true,
            "options": {
                "cwd": "packages/containers/azure-laravel-webapp",
                "commands": ["bash ./build.sh"]
            },
            "dependsOn": ["^build"],
            "inputs": ["default"],
            "outputs": ["{workspaceRoot}/dist/{projectRoot}/docker/**/*"]
        }
    }
}
