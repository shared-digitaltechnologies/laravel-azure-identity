#!/bin/sh
set -e

# OPTIMIZE THE APPLICATION
# shellcheck source=../startup/run-startup-optimisations.sh
. /usr/local/startup/run-startup-optimisations.sh

# RUN SUPERVISOR
/usr/bin/supervisord -n -c "${SUPERVISOR_CONFIG_FILE:-/etc/supervisord.conf}" "$@"
