{
    "name": "containers-azure-otelcol-agent",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "tags": ["docker", "platform:azure"],
    "sourceRoot": "packages/containers/azure-otelcol-agent",
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "cache": true,
            "options": {
                "cwd": "packages/containers/azure-otelcol-agent",
                "commands": ["bash ./build.sh"]
            },
            "inputs": ["default"],
            "outputs": ["{workspaceRoot}/dist/{projectRoot}/docker/**/*"]
        }
    }
}
