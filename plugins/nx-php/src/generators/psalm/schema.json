{
    "$schema": "http://json-schema.org/schema",
    "$id": "Psalm",
    "title": "Psalm Generator",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "The name of the project to which you want to add psalm.",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "To which php-project would you like to add the psalm typechecker?",
            "x-dropdown": "projects"
        },
        "vendorDir": {
            "type": "string",
            "description": "Location of the composer vendor dir. (relative to the workspace root)"
        },
        "sourceDir": {
            "type": "string",
            "description": "Location of the source root of this project. (relative to the workspace root)"
        },
        "composerJson": {
            "type": "string",
            "description": "Location of the composer json file. (relative to the workspace root)"
        }
    },
    "required": ["project"]
}
