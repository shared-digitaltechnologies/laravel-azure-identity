{
    "$schema": "http://json-schema.org/schema",
    "$id": "Phpunit",
    "title": "Phpunit generator",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "The name of the project to which you want to add phpunit.",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "Which project should use phpunit?",
            "x-dropdown": "projects"
        },
        "testNamespace": {
            "type": "string",
            "description": "The root namespace for the test classes."
        },
        "vendorDir": {
            "type": "string",
            "description": "The location of the composer vendor directory (relative to the workspace root!)"
        },
        "sourceDir": {
            "type": "string",
            "description": "The location of the source directory of the project (relative to the workspace root!)"
        }
    },
    "required": ["project"]
}
