{
    "$schema": "http://json-schema.org/schema",
    "version": 2,
    "title": "Bicep Lint",
    "description": "Lints bicep files using the `az bicep lint` command.",
    "type": "object",
    "properties": {
        "azPath": {
            "type": "string",
            "description": "Path to the Azure CLI (`az`) command."
        },
        "file": {
            "type": "string",
            "description": "The path to the Bicep file to lint.",
            "x-completion-type": "file",
            "x-completion-glob": "*.bicep",
            "x-priority": "important"
        },
        "noRestore": {
            "type": "boolean",
            "default": false,
            "description": "When set, builds the bicep file without restoring external modules."
        }
    },
    "required": ["file"]
}
